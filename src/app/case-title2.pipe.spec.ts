import { CaseTitle2Pipe } from './case-title2.pipe';

describe('CaseTitle2Pipe', () => {
  it('create an instance', () => {
    const pipe = new CaseTitle2Pipe();
    expect(pipe).toBeTruthy();
  });
});
